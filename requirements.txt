# Core UI
customtkinter>=5.2

# XML/report processing
pandas>=2.0
openpyxl>=3.1

# PDF rendering and text extraction
pymupdf>=1.24        # import fitz — renderizado, zoom, copia de texto con clic derecho

# Hacienda API + HTTP requests
requests>=2.31

# Security — encryption and credential storage
cryptography>=42.0
keyring>=24.0

# Build — empaquetar como .exe (solo en entorno de desarrollo)
pyinstaller>=6.0
